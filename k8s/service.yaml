apiVersion: v1
kind: Service
metadata:
  name: hf-home-finance-svc

spec:
  selector:
    app: home-finance-service

  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP

  type: ClusterIP